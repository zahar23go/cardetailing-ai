/**
 * Отчёты — /analytics/reports
 * Пример переноса: контент живёт в page, layout — OwnerDashboard + <Outlet />.
 */
import ReportManager from '../../../components/ReportManager';

export default function ReportsPage() {
  return <ReportManager />;
}
